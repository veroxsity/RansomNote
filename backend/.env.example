# Backend environment variables
# Comma-separated list of allowed frontend origins for Socket.IO CORS
# e.g. http://localhost:3000,https://ransomnotes.yourdomain.com
FRONTEND_ORIGIN=http://localhost:3000
